name: epub-translator
x-logging: &logging
  driver: json-file
  options:
    max-size: 10m
    max-file: "3"
x-app: &app
  build: .
  image: ${LIBRIS_IMAGE:-epub-translator:local}
  env_file: ${LIBRIS_ENV_FILE:-.env}
  environment:
    DATABASE_URL: postgresql+psycopg://translator:${POSTGRES_PASSWORD:?Run scripts/setup.py}@database:5432/translator
    DATA_DIR: /data
    # Temporary files (batch export ZIPs, EPUBCheck copies) go to the data volume, not to memory.
    TMPDIR: /data/tmp
  volumes:
    - books:/data
  # Nothing in the image is written at run time: code, prompts and interface are read-only; the
  # data volume and a small /tmp (JVM, libraries) are the only writable places.
  read_only: true
  tmpfs:
    - /tmp:size=256m,mode=1777
  restart: unless-stopped
  security_opt:
    - no-new-privileges:true
  cap_drop: [ALL]
  pids_limit: 512
  logging: *logging
services:
  codex:
    profiles: [codex]
    build:
      context: .
      dockerfile: codex_bridge/Dockerfile
    image: ${LIBRIS_CODEX_IMAGE:-epub-translator-codex:local}
    stop_grace_period: 30s
    healthcheck:
      test: [CMD, python, -c, "import urllib.request,os; r=urllib.request.Request('http://127.0.0.1:8092/health',headers={'Authorization':'Bearer '+os.environ['CODEX_BRIDGE_TOKEN']}); urllib.request.urlopen(r)"]
      interval: 10s
      timeout: 5s
      retries: 10
    environment:
      CODEX_BRIDGE_TOKEN: ${CODEX_BRIDGE_TOKEN:-}
    volumes:
      - codex-state:/state
    tmpfs:
      - /tmp
    read_only: true
    restart: unless-stopped
    security_opt:
      - no-new-privileges:true
    cap_drop: [ALL]
    pids_limit: 256
    logging: *logging
  database:
    image: postgres:17-bookworm
    environment:
      POSTGRES_USER: translator
      POSTGRES_DB: translator
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:?Run scripts/setup.py}
    volumes:
      - database:/var/lib/postgresql/data
    healthcheck:
      test: [CMD-SHELL, "pg_isready -U translator -d translator"]
      interval: 5s
      timeout: 5s
      retries: 20
    # The entrypoint starts as root only to fix the data directory's owner, then drops to `postgres`
    # (gosu, a setuid() call that no-new-privileges allows): these five capabilities are all it needs.
    security_opt:
      - no-new-privileges:true
    cap_drop: [ALL]
    cap_add: [CHOWN, DAC_OVERRIDE, FOWNER, SETGID, SETUID]
    read_only: true
    tmpfs:
      - /var/run/postgresql:size=16m
      - /tmp:size=64m,mode=1777
    shm_size: 256m
    pids_limit: 256
    logging: *logging
    restart: unless-stopped
  migrate:
    <<: *app
    command: [alembic, upgrade, head]
    restart: "no"
    depends_on:
      database:
        condition: service_healthy
  api:
    <<: *app
    ports:
      - "${BIND_ADDRESS:-127.0.0.1}:${PORT:-8088}:8088"
    depends_on:
      migrate:
        condition: service_completed_successfully
    healthcheck:
      test: [CMD, python, -c, "import urllib.request; urllib.request.urlopen('http://127.0.0.1:8088/health')"]
      interval: 15s
      timeout: 5s
      retries: 10
  worker:
    <<: *app
    command: [python, -m, app.jobs.worker]
    stop_grace_period: 30s
    depends_on:
      api:
        condition: service_healthy
volumes:
  codex-state:
  books:
  database:
